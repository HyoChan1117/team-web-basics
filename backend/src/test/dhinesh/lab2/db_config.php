<?php
    // db config
    $hostname = 'db';
    $username = 'root';
    $password = 'root';
    $database = 'bar';

    // db connect 
    $db_conn = new mysqli($hostname, $username, $password, $database);

?>
