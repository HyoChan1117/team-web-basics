// for of 문
// 배열, 문자열, Set, Map 등 반복 가능한(iterable) 객체 순회

// 배열
const fruits = ["apple", "banana", "cherry"];

for (const item of fruits) {
    console.log(item);
}

// 문자열
for (const ch of "CAT") {
    console.log(ch);
}