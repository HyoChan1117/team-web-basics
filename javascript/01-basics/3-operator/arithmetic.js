// 산술 연산자
// +, -, *, /, %, **, ++, --
let a = 5 + 2;  // 덧셈
let b = 5 - 2;  // 뺄셈
let c = 5 * 2;  // 곱셈
let d = 5 / 2;  // 나눗셈
let e = 5 % 2;  // 나머지
let f = 5 ** 2; // 제곱

console.log(a++);  // 후위 증가 (출력 후 증가)
console.log(--b);  // 전위 감소